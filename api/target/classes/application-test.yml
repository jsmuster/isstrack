spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/issuetracker_test
    username: issuetracker
    password: issuetracker
  jpa:
    hibernate:
      ddl-auto: validate

app:
  jwt:
    secret: "TEST_SECRET"
    issuer: "issue-tracker"
    expirationMinutes: 60

websocket:
  auth:
    allow-query-token: false
